{% extends "base.twig" %}

{% block content %}

    <div class="page-text content">
        <div class="container">
            <h2>{{ wp_title }}</h2>
            <div>{{ post.post_content }}</div>
        </div>
    </div>

{% endblock %}